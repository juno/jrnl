<h1>Administration</h1>
<%= content_tag(:div,
                link_to('Sign out', destroy_user_session_path, method: :delete),
                class: 'sign-out') %>
<%= content_tag(:div, link_to('New post', new_post_path)) %>
<table class="posts">
  <thead>
    <th>Created at</th>
    <th>Title</th>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <%= content_tag(:tr, post, id: dom_id(post)) do %>
        <td style="white-space:nowrap">
          <%= post.created_at.strftime('%Y-%m-%d') %>
        </td>
        <td>
          <%= post.title %><br>
          <%= link_to('Show', post) %>
          -
          <%= link_to('Edit', edit_post_path(post)) %>
          -
          <%= link_to('Delete',
                      post,
                      method: :delete,
                      data: { confirm: 'Are you ok?' }) %>
        </td>
      <% end %>
    <% end %>
  </tbody>
</table>
<%= paginate @posts %>
